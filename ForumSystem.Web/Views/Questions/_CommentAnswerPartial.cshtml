@using ForumSystem.Web.InputModels.Questions
@model CommentAnswerInputModel
@using (Ajax.BeginForm("CommentAnswer", new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.InsertAfter, UpdateTargetId = "answer-comments" }))
{
    @Html.HiddenFor(m => m.AnswerId);
    @Html.TextBoxFor(m => m.CommentContent);
    <input type="submit" class="btn-default" value="Comment" />
}
